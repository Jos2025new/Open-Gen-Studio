# Trazabilidad de cambios

Para cada cambio: qué hace, dónde está, qué lo verifica y qué queda sin verificar. Sirve para saber si un cambio nuevo rompió algo que antes funcionaba. El plan paso a paso está en `AGENTS.md`, el historial en `PROGRESS.md` y el detalle de proveedores en `MODEL_VALIDATION.md`.

## Cómo comprobar que nada se rompió

| Comprobación | Comando | Qué detecta |
| --- | --- | --- |
| Tipos | `npx tsc --noEmit -p .` | Contratos rotos entre módulos. |
| Tests unitarios | `npm test` | Lógica de precios, parámetros, rutas de entradas, polling, payloads (tests con nombre en la tabla). |
| **Red de regresión de proveedores** | `npm test` (incluye `tests/live-snapshot.test.ts`) | Cómo se lista e interpreta **cada** variante de las familias prioritarias (~560: Atlas 162, fal 257, NanoGPT 138). Compara con `tests/fixtures/live/expected.txt`. Si un cambio altera un modelo, el test falla y muestra la diferencia. |
| Aceptar un cambio intencionado | `npx vitest run -u tests/live-snapshot.test.ts` y revisar `git diff tests/fixtures/live/expected.txt` | El diff se revisa y se sube en el mismo commit que el cambio de código. Así cada commit dice exactamente qué modelos cambiaron. |
| Catálogos nuevos del proveedor | `npm run snapshot:models`, luego lo anterior | Descarga de nuevo los catálogos y esquemas públicos (sin clave ni coste). El diff muestra qué cambió **en el proveedor**. Conviene hacerlo en un commit aparte del de código, para no mezclar causas. |
| Navegador | `npm run dev` (puerto 5173) | Interfaz y payloads reales con `fetch` interceptado (sin gastar). Pasos concretos en cada fila. |

Familias cubiertas por la red: `scripts/model-families.json`. Para ampliar la cobertura se añade el patrón y se regenera.

## Registro

| Commit | Cambio | Archivos | Verificación automática | Verificación manual | Sin verificar / límites |
| --- | --- | --- | --- | --- | --- |
| `97aed17` | Un fallo al consultar un trabajo remoto no lo da por perdido: reintentos con espera creciente y límite local de espera. `JobFailedError` solo cuando el proveedor confirma el fallo. **Check again** reanuda. Duración `-1` estimada con la máxima y mostrada como "Auto". | `lib/http.ts`, `providers/{shared,atlas,fal,nanogpt}.ts`, `jobs.ts`, `pricing.ts`, `costs.ts`, `GenerationCard.tsx` | `engine.test.ts`: *remote job polling* (4), *prices an automatic duration* | Navegador con `fetch` simulado: 503→completed, 401 conserva el trabajo, `failed` lo cierra, cancelar lo conserva. | `/generate-video/recover` de NanoGPT no integrado. |
| `a8604c9` | Validación de modelos de vídeo. `needsVideo` separado de `acceptsVideo`. Clasificación de Atlas por el nombre del endpoint. Referencias hacia modelos de vídeo (`routeVideoInputs`). Slots `refVideos` y `mixedRefs` (`refers`). En NanoGPT, `referenceImages`/`referenceVideos`/`last_image`. `fixed` para campos obligatorios, `disabled` omitido, límite de 4 MB. | `params.ts`, `jobs.ts`, `actions.ts`, `Composer.tsx`, `ModelList.tsx`, `SettingsPanel.tsx`, `catalog.ts`, `providers/*` | `providers.test.ts`: *reference inputs*, *video input routing*, *model classification*, *request payloads*; red de regresión. | Payloads interceptados: Atlas `refers`, NanoGPT `imageDataUrl` y `referenceImages`. | Campos de NanoGPT con data URL, sin generación real. |
| `6def2d0` | Validación de familias de imagen. Tamaños en píxeles como encuadre (con su escala). `size` de texto libre con opciones y "Auto" no enviado (`omit`). Campo de imagen obligatorio preferido. `missing` bloquea con aviso. En NanoGPT, `resolution` mixto y endpoints solo de texto. `isAutoOption`. | `params.ts`, `jobs.ts`, `actions.ts`, `providers/{atlas,fal,nanogpt}.ts` | `providers.test.ts`: *image schemas* (4); red de regresión. | Con 16:9, payloads interceptados: `2048x1152`, `2048*1152`, `1536*864` y `16:9`. | Sin generación real. |
| `175b4e0` | Red de regresión de proveedores: instantánea de esquemas reales y parse esperado. | `scripts/model-snapshot.mjs`, `scripts/model-families.json`, `tests/live-snapshot.test.ts`, `tests/fixtures/live/*` | La propia red: línea base del estado tras `6def2d0`. | — | La instantánea es del 2026-09-25; los catálogos cambian con el tiempo. |
| `327b549` | Bugs sin decisión: slot `source` (imagen de origen + referencias, Ideogram Character remix/edit); `max_images` como número de imágenes si no hay otro (Seedream sequential); listas simples del catálogo de NanoGPT como opciones (`rendering_speed`). Listado de fal robusto ante 429 y claves rechazadas. | `types.ts`, `params.ts`, `jobs.ts`, `actions.ts`, `providers/{shared,fal,atlas,nanogpt}.ts` | `providers.test.ts`: *sends the first image as source…*, *fal catalog listing*; red de regresión: diff de 9/6 líneas, solo los 8 modelos afectados; `MISSING` 21 → 20. | Navegador (payload interceptado): Ideogram Character remix envía `image_url` (primera imagen) y `reference_image_urls` (resto). | Seedream edit-sequential: el límite "referencias + generadas ≤ 15" no se valida en la app. El listado de fal no se pudo comprobar en el navegador: la IP seguía limitada por las descargas de la instantánea; cubierto por el test *fal catalog listing*. |
| `acfd4a2` | Operación **Extend video** (lista preferida por proveedor, fila en Ajustes, menú del vídeo, agente). Registro `modelRules.ts`: Seedance 2.5 recibe el clip como vídeo de referencia (edición 4–30 s, extensión 2–30 s). `InputError` con código. Al editar: `duration -1` si el modelo lo admite y proporción "auto/adaptive". NanoGPT: `mode` explícito. | `ops.ts`, `types.ts`, `modelRules.ts`, `errors.ts`, `jobs.ts`, `costs.ts`, `catalog.ts`, `providers/{registry,atlas,fal}.ts`, `store.ts`, `SettingsPanel.tsx`, `AssetActions.tsx` | `video-ops.test.ts` (4): reglas de Seedance en `opSpec`, payload con `reference_videos`, rechazo `[VIDEO_DURATION]` sin envío, `mode` de NanoGPT. Red de regresión: solo 3 modelos (Seedance 2.5 reference: `video-in`). | Navegador (vídeo del demo local, gratis): "Extend video" en el menú del vídeo; el formulario exige "Then"; fila en Ajustes → Operaciones. | Sin generación real de pago. Para la extensión se estima el precio de los segundos nuevos; Seedance cobra entrada + salida, así que la estimación queda por debajo. |
| `9372eea` | Keyframes (FLUX 3: `{image_url, frame_index}` a 24 fps, reparto según la guía de BFL, duración explícita) y clips recortados (`video_clips`: Gemini reference-developer, Nano Banana 2 reference-to-image, también en imagen). Vuelven a listarse los endpoints ocultos. Segundo o recorte editable en cada adjunto. Agente: `refs`/`times` en vídeo, reglas en el prompt y `capabilityHints` por modelo. Nodos: puertos de referencias, vídeos de referencia y clip. | `types.ts`, `params.ts`, `jobs.ts`, `actions.ts`, `plan.ts`, `executor.ts`, `flow/graph.ts`, `agent/{tools,context,runtime}.ts`, `providers/{shared,atlas,fal,types}.ts`, `Composer.tsx`, `composer.css`, `store.ts` | `keyframes-clips.test.ts` (7): colocación, recortes, payload completo de fal FLUX 3 (0/72/240 con un segundo fijado), plan del agente válido e inválido. Red de regresión: +2 modelos listados, `MISSING` 20 → 15, más la línea `inputs` que ve el agente para las 556 variantes. | Navegador (envío interceptado): FLUX 3 keyframes de Atlas con 3 imágenes → etiquetas 0,0/2,5/5,0 s; fijar 1,5 s → payload `frame_index` 0/36/120, `duration 5`; nodo de vídeo con los 5 puertos. | Sin generación real. Keyframes de 3 o más: experimental según BFL. En nodos no se pueden fijar segundos ni recortes. |
| `6af89c6` | Audio como asset (subida, disco, reproductor, galería, nodos) y como entrada: `audio` (lip-sync, banda sonora), `refAudios` (`reference_audios`, `audio_urls`, `reference_audio_urls`) y `refers` de tipo audio. `routeAudio`/`audioInputProblem` compartidos por composer, runner y plan. NanoGPT: `audio`, `audioDataUrl` (guía) y `referenceAudios` (supuesto, sin verificar). | `types.ts`, `params.ts`, `jobs.ts`, `actions.ts`, `plan.ts`, `flow/graph.ts`, `agent/{tools,context}.ts`, `providers/{types,shared,atlas,nanogpt}.ts`, `lib/media.ts`, `server/local-store.js`, `AssetMedia.tsx`, `GalleryPanel.tsx`, `GenerationInfo.tsx`, `Lightbox.tsx`, `Composer.tsx`, estilos | `audio.test.ts` (5): detección en esquemas reales, reparto y errores, payload completo del lip-sync de fal, `refers` con audio, planes del agente. Red de regresión: 85 líneas con los huecos de audio; `MISSING` 15 → 14 (lip-sync de fal). | Navegador: WAV de 2 s subido (`audio`, 2 s); lip-sync de NanoGPT con imagen y voz → payload interceptado `imageDataUrl` + `audioDataUrl`; galería con filtro Audio; visor con reproductor; nodo de audio conectado al puerto Audio y rechazado en el de referencias. Servidor local: audio/x-wav → `.wav` y vuelta. | `referenceAudios` en NanoGPT no está documentado. Sin generación real. |
| `5ad83f5` | Transcripción: operación Transcribe (audio → texto) con NanoGPT `/api/transcribe` (multipart ≤3 MB, trabajos 202 con `/transcribe/status`, reanudables). Generaciones de texto (`kind: 'text'`, `text`). Tarjeta con Copy / Use as prompt, nodo cuya salida alimenta un Prompt, `prompt_from` del agente, selector en Ajustes, catálogo de transcriptores en la red de regresión. Correcciones: `runInNodes` enviaba referencias de vídeo/audio al puerto First frame; un nodo de texto terminado se re-ejecutaba; precio mínimo mostrado como gratis; `formatUsd` con costes mínimos. | `types.ts`, `ops.ts`, `jobs.ts`, `catalog.ts`, `costs.ts`, `executor.ts`, `actions.ts`, `flow/graph.ts`, `providers/{types,nanogpt,atlas,fal,registry}.ts`, `store.ts`, `lib/format.ts`, `GenerationCard.tsx`, `nodes.tsx`, `SettingsPanel.tsx`, `AssetActions.tsx`, `scripts/model-snapshot.mjs`, estilos | `transcribe.test.ts` (6): catálogo sin clonación de voz, transcripción síncrona con FormData y coste, trabajo 202 con diarización, fallo confirmado y `[AUDIO_TOO_LARGE]` sin envío, precio mínimo, texto hacia un Prompt. `engine.test.ts`: *money formatting*. Red de regresión: lista de 13 transcriptores. | Navegador (envío interceptado): WAV de 3 s → estimación $0,0001 aproximada, Whisper Large V3 por defecto; FormData `model`/`language`/`input.wav`; tarjeta con el texto y coste "<$0.001"; Use as prompt → composer; selector "Auto · Whisper Large V3 · $0.000495/min". | Sin transcripción real de pago. Transcribir vídeo (xAI, GPT-4o Mini lo admiten) no está expuesto aún. |
| `f46c736` | Sujetos de Kling (biblioteca por sesión, @Nombre → elementos de Atlas o fal, voz por sujeto y creación de voz con fal), multiplano (`multi_prompt` + `multi_shot`/`shot_type`, prompt excluyente en fal) y selección múltiple (`voice_ids` de Grok) en `settings.extras`. Agente con sujetos y `shots`. Títulos de tarjetas por planos. Nombres accesibles en los chips de icono. | `types.ts`, `params.ts`, `jobs.ts`, `actions.ts`, `plan.ts`, `ops.ts`, `catalog.ts`, `agent/{tools,context}.ts`, `providers/{types,shared,atlas,fal}.ts`, `MediaControls.tsx`, `GenerationInfo.tsx`, `AssetActions.tsx`, `composer.css`, `scripts/model-snapshot.mjs` | `subjects-shots.test.ts` (10): detección en esquemas reales, menciones (orden, nombres largos, sin plantilla), planos, elementos de Atlas, payload completo fal con voz, `[SUBJECT_INCOMPLETE]`, creación de voz y `[VOICE_DURATION]`, planes del agente. Red de regresión: `elements`/`shots` en Kling V3/O3, `voice_ids` como `multi`. | Navegador (envío interceptado): sujeto "Mia" desde un adjunto → prompt `<<<element_1>>>` y `elements` con la imagen subida; editor de planos 2 + 3 s → `multi_prompt` con `index`, `multi_shot`, `shot_type`; voces de Grok seleccionables. | Sin generación real ni creación real de voz (cuesta en fal). Fallo hallado en los tests: `@Ana Maria` también se leía como `@Ana` (corregido); la sintaxis de Atlas se tomaba de una descripción recortada (ahora se deduce del estilo; el script de instantánea guarda descripciones de hasta 2.000 caracteres en la próxima regeneración). |
| `71e4d97` | Máscaras: modo Mask en Sketch (también desde el menú de una imagen), Edit region y Remove object con confirmación de coste; `InputSlots.mask` con convención blanco/alfa; solo modelos con máscara (listas preferidas y filas de Ajustes filtradas); rechazos con código. | `types.ts`, `params.ts`, `ops.ts`, `jobs.ts`, `catalog.ts`, `plan.ts`, `agent/{tools,context}.ts`, `providers/{types,shared,atlas,registry}.ts`, `lib/media.ts`, `store.ts`, `SketchEditor.tsx`, `AssetActions.tsx`, `SettingsPanel.tsx`, `GalleryPanel.tsx`, `shell.css` | `masks.test.ts` (5): conversión blanco→alfa, máscara tal cual a Ideogram y alfa a GPT Image, Remove object dedicado o por inpainting, `[MASK_MISSING]`/`[MASK_SIZE]`/`[MASK_UNSUPPORTED]` sin envío, exclusión de menús/agente. Red de regresión: 13 modelos con `mask` y su convención; `MISSING` 14 → 6. | Navegador (envío interceptado): máscara pintada con el ratón sobre una foto de 640×480 → Edit region con GPT Image 2.5 Flare ($0,005) → máscara subida de 640×480, transparente en lo pintado y opaca fuera. Corregido al verificar: paleta visible en modo máscara y trazo poco visible sobre rojo. | Sin edición real de pago. |
| `a1f5252` | Estilo: colores y fondo de Recraft, paletas de Ideogram (preset o colores con peso), `style_codes` validados (8 hex), `style_id` y `model_id` como texto; estilos Recraft V4 creados desde imágenes adjuntas (fal `recraft/v4/create-style`, con confirmación) y guardados en la sesión; agente con colores en hex. | `types.ts`, `params.ts`, `plan.ts`, `jobs.ts`, `catalog.ts`, `actions.ts`, `agent/context.ts`, `providers/{types,fal}.ts`, `MediaControls.tsx`, `composer.css` | `style.test.ts` (4): detección en Recraft e Ideogram reales, conversión hex↔RGB y paletas, validación de códigos, Ideogram custom ejecutable con `model_id`, creación de estilo de extremo a extremo con fetch simulado. Red de regresión: `colors` (18), `background_color` (15), `style_codes`/`color_palette` (8), `style_id` (7), `model_id` (1); `MISSING` 6 → 5. | Ninguna: por decisión del usuario no se usó el navegador en esta fase. | Controles de estilo y creación de estilos sin verificar visualmente ni con generación real. Estilos V3 (ZIP) no se crean en la app. |
| `5cab903` | Avatares parlantes de Atlas: InfiniteTalk (`image` + `audio`) y OmniHuman 1.5 (`image_url` + `audio_url`). La categoría `AUDIO-TO-VIDEO` se lista como imagen + audio → vídeo; la clave `audio` de tipo texto cuenta como pista; el estado se consulta en `data.urls.get` que devuelve Atlas (OmniHuman responde en `/model/result/{id}`, no en `/prediction`). Imagen de inicio obligatoria (`firstFrame.required`): el composer y el agente la exigen antes de enviar ("needs a start image."). | `types.ts`, `params.ts`, `providers/atlas.ts`, `scripts/model-{families.json,snapshot.mjs}`, `tests/fixtures/live/atlas.json` | `audio.test.ts` (+1): OmniHuman de extremo a extremo con fetch simulado (sube imagen y audio, cuerpo con `image_url`/`audio_url`, sondeo en la URL de Atlas) y rechazo sin imagen. Red de regresión: 2 modelos nuevos y `required` en 64 slots de imagen de inicio (image-to-video existentes). 85 tests. | Ninguna: sin navegador por decisión del usuario. | Sin generación real. La marca `required` también afecta a los image-to-video existentes (antes fallaban en el proveedor; ahora antes de enviar). |
| *(4c-2)* | Música y letras (MiniMax en Atlas, `/model/generateAudio`). `MediaKind` gana `audio` (el compilador guió cada sitio); `ModelSummary.textOutput` (Lyrics responde con texto: `lyrics_result` → "# Título / Style: … / letra"). Atlas lista `Audio` + `TEXT-TO-AUDIO` solo de las familias confirmadas. Parámetros: `lyrics` (texto multilínea con etiquetas de sección) y `title` en `extras`; `format` sin `pcm`. `songProblem`: reglas `allOf` del esquema comprobadas antes de enviar (`LYRICS_MISSING`, `LYRICS_CONFLICT`, `PROMPT_MISSING`, `LYRICS_TOO_LONG`) en composer, plan y runner. Composer: modo Audio, chip Lyrics, "Use as lyrics" (quita "write lyrics for me"). Agente: paso `audio` con `params` y `lyrics_from`, contexto y guía de prompts. Nodos: nodo Audio (puertos Prompt y Lyrics, opciones del modelo en Settings, letra editable), salida de texto si el modelo es Lyrics; `runsGeneration` sustituye 12 comprobaciones repetidas. Hallado: un valor estructurado podía quedar también en `advanced` (inofensivo, ya filtrado). | `types.ts`, `params.ts`, `plan.ts`, `jobs.ts`, `executor.ts`, `catalog.ts`, `actions.ts`, `flow/{graph,actions}.ts`, `agent/{tools,context}.ts`, `providers/{atlas,registry}.ts`, `lib/media.ts`, `store.ts`, `Composer.tsx`, `ModeMenu.tsx`, `MediaControls.tsx`, `ModelList.tsx`, `GenerationCard.tsx`, `PlanCard.tsx`, `nodes.tsx`, CSS | `music.test.ts` (11): listado y parseo reales, reglas con código, canción de extremo a extremo (cuerpo, asset de audio), letra como texto y su precio, rechazo sin petición, reanudación tras recarga, composer en modo Audio, plan letra → canción y su grafo, ejecución encadenada que envía solo la letra, puerto Lyrics desde un nodo terminado. Red de regresión: 2 modelos de audio y la marca `TEXT-OUT`. 96 tests, typecheck y `vite build`. | Ninguna: sin navegador por decisión del usuario. | UI (modo Audio, chip Lyrics, nodo Audio) sin verificar visualmente; sin generación real. Solo MiniMax en Atlas: otras familias de audio esperan decisión. |
| *(3D P1–P6)* | Generación 3D como un tipo más (`MediaKind`/asset `model3d`). Atlas: Tripo H3.1 texto/imagen y Seed3D 2.0 (categorías `*-TO-3D`, solo esas familias; `generateImage`; Seed3D consulta `/model/result/{id}` y pide `file_format: glb`; Tripo lee `files`/`thumbnail`). NanoGPT: TRELLIS.2, Seed3D 2.0 y Meshy 7.1 (texto, imagen, multivista) desde `/v1/3d-models`, envío por `/generate-video` con `imageDataUrl` y consulta `/video/status`; precio por variante (`per_run_by_variant` + `variant3dKey`). Salidas tipadas por sus bytes (`sniffModelMime`): cada archivo de modelo es un asset (GLB, ZIP, FBX), el render es miniatura (data URL JPEG) y un clip de vista previa se descarta; sin archivo 3D → fallo. Reglas con fuente y código (`MODEL3D_RULES`: `PROMPT_TOO_LONG`, `MODEL3D_IMAGE_REQUIRED`, `IMAGE_ASPECT`, `IMAGE_TOO_LARGE`, `MODEL3D_VIEWS`…). Composer (modo 3D, importar GLB a la galería), agente (paso `model3d`), nodos (nodo 3D con puerto de imágenes), visor `model-viewer` cargado solo en el lightbox, ZIP extraído con límites. | `lib/model3d.ts`, `providers/{atlas,nanogpt,shared}.ts`, `jobs.ts`, `modelRules.ts`, `costs.ts`, `params.ts`, `plan.ts`, `actions.ts`, `agent/{tools,context}.ts`, `flow/*`, `components/{assets,composer,gallery,node,chat}/*` | `tests/3d-contracts.test.ts` (11: catálogo solo con los 8 endpoints, esquemas reales de Atlas y catálogo NanoGPT, precios por variante, Tripo GLB+FBX sin asset de imagen, Seed3D ZIP por `/result`, TRELLIS y Meshy multivista, solo-render falla, reanudar sin reenviar, reglas, plan), `tests/model3d.test.ts` (GLB/ZIP) | Pendiente (usuario): generación real en cada endpoint, visor en navegador, recarga y descarga. | NanoGPT no documenta la forma de salida 3D ni la clave multivista: se envía `imageDataUrl` (documentado) + `imageDataUrls` (sin verificar) y se buscan URLs en claves comunes. Hosts de descarga 3D de Atlas no están en la lista del relay `/x/media`: si falla CORS, el asset queda con URL remota (24 h en Seed3D). FBX/OBJ/USD se descargan pero no se previsualizan. `texture_image` de Meshy (URL) no se expone. La red de regresión (`expected.txt`) aún no incluye los modelos 3D. |
| *(P7)* | Exportar documentos del Designer: PNG, JPG (fondo blanco si es transparente), SVG por capas (grupos `inkscape:groupmode="layer"` con nombre, opacidad, `mix-blend-mode`; `rect`/`ellipse`/`line`; texto editable con `tspan` por línea y base = caja + relleno + `emHeightAscent`; imágenes como data URL PNG; `clipPath` de la página) y PDF desde el mismo SVG (jsPDF 4.2.1 + svg2pdf.js 2.8.1, MIT, `import()` diferido). | `engine/design/{export,actions,render}.ts`, `components/designer/DesignerWorkspace.tsx` | `tests/design-export.test.ts` (orden y nombres de capas, capas ocultas fuera, opacidad/mezcla, escape XML, sin `blob:`, formas como el renderer, posiciones de texto) | Pendiente (usuario): abrir SVG/PDF en Inkscape y comparar con el PNG. | Fuentes: el SVG nombra la familia (editable si está instalada); el PDF usa las fuentes estándar de jsPDF. Soporte de `mix-blend-mode` en Inkscape y svg2pdf sin comprobar. |
| *(P8)* | Lineart editable: `Stroke` (gesto con presión + estilo) en `VectorLayer.strokes`; `design/strokes.ts` (perfect-freehand 1.2.3, MIT; contorno y path en caché `WeakMap` por objeto; mover, escalar, doblar con caída, reestilizar); herramienta Lineart con `getCoalescedEvents`, trazo en curso fuera del documento (un paso de deshacer por gesto); controles en la barra y en Propiedades; export SVG como `<path>` relleno. | `engine/design/{strokes,brushTextures,doc,render,export,rules}.ts`, `engine/types.ts`, `store/store.ts`, `components/designer/{Stage,ToolRail,LayersPanel,StrokeStyleFields,DesignerWorkspace}.tsx` | `tests/strokes.test.ts` (mismo gesto → mismo contorno, reestilo sin perder el gesto, doblado con caída, mover/escalar/duplicar con la capa, SVG) | Pendiente (usuario): dibujar con tableta y ratón, deshacer/rehacer, recarga, SVG en Inkscape. | El SVG guarda el contorno, no la curva de presión: en Inkscape se edita como path, no como pincel. Doblar mueve puntos del gesto (no hay nodos Bézier). |
| *(P9)* | Pincel con textura a lo largo del trazo: `design/brushTextures.ts` (estampas procedurales, colocación por distancia/tangente/presión con jitter por semilla, tope de 1500 estampas por trazo, caché por trazo en `WeakMap`, tinte por color), controles de textura en la barra y en Propiedades, SVG con `<defs><image>` + `<use>` por estampa. | `engine/design/brushTextures.ts`, `components/designer/StrokeStyleFields.tsx` | `tests/strokes.test.ts` (misma semilla → mismas estampas; espaciado; doblar gira las estampas; tope; una imagen por trazo reutilizada) | Pendiente (usuario): aspecto y fluidez al dibujar, Inkscape, peso del SVG. | La caché se dibuja a ≤2× de la resolución del documento: con zoom alto puede verse suave. En el SVG cada estampa es un `<use>`: trazos muy largos pesan más (máx. 1500 por trazo). No hay edición del motor de pincel en Inkscape. |
| *(A1–A4)* | Agente sin latencia añadida: `find_models` (búsqueda local en el catálogo pulido: familias prioritarias + 3D elegidos, proveedores conectados, sin `missing`) y "Did you mean" en el validador; fila de estado (Working on it / Drafting the plan / Checking the plan) desde eventos del stream; comentario sobre un plan pendiente = revisión (`propose_plan.revision`), si no se cierra; casillas por paso con arrastre de dependencias. | `engine/agent/{modelIndex,tools,runtime,context}.ts`, `engine/plan.ts`, `engine/providers/llm.ts`, `components/chat/{FeedList,PlanCard}.tsx`, `styles/chat.css` | `tests/model-index.test.ts` (catálogo real: "seedance 2.0 fast" → `nanogpt::bytedance-seedance-2-0-fast`; nada fuera de las familias), `tests/agent-status.test.ts` (aviso con el primer fragmento; cuerpo sin cambios), `tests/plan-revision.test.ts` (una llamada por comentario; revisión reemplaza, otra petición o texto cierran; casillas y mensaje al agente) | Pendiente (usuario): repetir la prueba real Kling → Seedance 2.0 Fast; ver el indicador; desmarcar pasos. | `find_models` añade una vuelta al modelo solo cuando el usuario nombra un modelo fuera de la lista corta. La decisión revisión/nueva petición la toma el agente (campo `revision`). |
| *(R0)* | Medición sin cambiar el comportamiento: `agent/metrics.ts` guarda por petición (`Session.agentMetrics`, últimas 50) llamadas al LLM, tokens, coste, tiempo del agente hasta la primera salida y hasta el plan (sin contar el del usuario), rondas de preguntas, planes, revisiones, rechazos del validador, `find_models`, modelos, estimado frente a aprobado y desenlace (aprobado, cancelado, sustituido). Banco `tests/bench/agent-bench.test.ts`: 9 peticiones fijas contra el LLM real hasta el plan; catálogo desde la red de regresión (`tests/fixtures/live/serve.ts`, compartido con `live-snapshot`) y cualquier otra petición bloqueada. | `agent/metrics.ts`, `agent/runtime.ts`, `types.ts`, `tests/agent-metrics.test.ts`, `tests/bench/`, `tests/fixtures/live/serve.ts` | `tests/agent-metrics.test.ts` (6: contadores de una petición con revisión y aprobación, preguntas en la misma petición, sustituida, cancelada, modo Auto, nada llega al modelo); suite completa. Banco probado con clave inválida: carga el catálogo, bloquea el resto y escribe el JSON. | — | Falta la tabla del antes: el banco necesita la clave del LLM y permiso del usuario; desde la nube `nano-gpt.com` está fuera de la lista de red. Los casos con imagen necesitan `bench/fixtures/character.jpg` (o `BENCH_IMAGE`). Las preguntas se responden con la primera opción. Una llamada que falla no cuenta en `llmCalls`. |
| *(R1+R2)* | Ruta estándar y reglas de prompting en `SYSTEM_PROMPT`; familias ("Wan 3") resueltas en la app (`familyRef`, `plan.ts`); Wan 3 primero en vídeo; resolución intermedia por defecto en vídeo (`mediumResolution`); `-spicy` fuera del índice salvo que se nombren. | `agent/context.ts`, `plan.ts`, `params.ts`, `providers/registry.ts`, `agent/modelIndex.ts` | `tests/agent-route.test.ts`, `tests/model-index.test.ts` (familia con y sin imagen, proveedor seleccionado, ref completa, spicy) | — | Efecto en el agente real sin medir (banco R8). Calidad = solo resolución y exclusión de `-spicy`: decisiones provisionales. |
| *(R3)* | Límite del prompt leído del esquema (`maxLength` o "up to / max N characters"; no las recomendaciones) en `slots.promptMax`, validado en el plan y antes de enviar (`PROMPT_TOO_LONG`). Sintaxis de citas del propio esquema (`slots.promptRefs`), que gana a la de la familia. Protocolos por familia (Seedance `@Image1`, Wan `@Image1 defines…`, MiniMax H3 `<Picture 1>`) en `REFERENCE_PROTOCOLS` con fuente, una vez en el prompt del sistema. `best_for`/`avoid_for` (`MODEL_FITS`) en la lista corta, el modelo seleccionado y `find_models`. | `modelRules.ts`, `params.ts`, `plan.ts`, `jobs.ts`, `agent/context.ts`, `agent/modelIndex.ts`, `tests/fixtures/live/expected.txt` | `tests/agent-route.test.ts` (límite, cita, rechazo, fit, protocolos); red de regresión: 170 modelos ganan línea de prompt. Cobertura del límite: Atlas 55/166, fal 107/257, NanoGPT 0/138 (su catálogo no describe el prompt). Sintaxis en esquema: 24 endpoints. | — | fal MiniMax H3 pide "Image 1" en su esquema frente a `<Picture 1>` de la guía oficial: gana el esquema del endpoint. Gemini Omni Flash sin nota (sin verificar). Guías fuente de Wan/MiniMax en la máquina del usuario. |
| *(R7)* | Un paso de vídeo sin proporción que parte de una imagen (primer fotograma o primera referencia: asset o paso de imagen) usa la proporción más cercana de esa imagen entre las que admite el modelo; se anota como ajuste. | `plan.ts` | `tests/agent-route.test.ts` (retrato → 9:16 con nota; la del paso gana; sin imagen, la de siempre) | — | Solo vídeo (los modelos 3D no tienen proporción). |
| *(R9)* | Un paso del plan que edita, amplía o extiende un vídeo se estima igual que la operación directa: `videoOpSettings`/`videoOpSeconds` (en `jobs.ts`) los comparten `opSpec` y `estimateSteps`. Edición y upscale por la duración del clip; extensión por los segundos nuevos. Si el clip sale de otro paso, se usa la duración de ese paso y se marca aproximado. | `jobs.ts`, `executor.ts` | `tests/video-ops.test.ts` (mismo precio por ambas vías; 12 s editados = 1,20 USD, 5 s extendidos = 0,50 USD a 0,10 USD/s) | — | El esquema del modelo de la operación se lee de la caché del catálogo; si aún no cargó, se usan valores por defecto (como antes). |
| *(R6)* | `ask_questions` gana `default` por pregunta (la opción recomendada); la app la guarda solo si es una de las opciones y la tarjeta la muestra marcada, así que un clic en Continue acepta todo. La descripción de la herramienta pide todas las preguntas juntas y no repetir lo ya resuelto. El banco responde con la opción por defecto. | `agent/tools.ts`, `agent/runtime.ts`, `types.ts`, `components/chat/QuestionsCard.tsx`, `tests/bench/agent-bench.test.ts` | `tests/agent-metrics.test.ts` (default válido se guarda; uno ajeno a las opciones se descarta) | Tarjeta sin probar en navegador. | El planificador local (sin LLM) no marca opciones. |
| *(R5)* | `Workflow.skill`: cada workflow recomienda una skill (Image → Video y Shot sequence → Cinematic video; Product ad pack → Product photography; Character sheet → Character consistency; Storyboard → Storyboard; Social format set → Social ad; Poster layout → Poster & typography). `activeSkill` la aplica si el usuario no eligió otra (agente con LLM y planificador local). | `skills.ts`, `agent/context.ts`, `agent/runtime.ts` | `tests/agent-route.test.ts` (todas existen; la elegida gana) | — | El selector del composer no muestra la skill implícita. |

## Estado de la línea base (antes de cubrir lo que faltaba)

Tras la fase 7 quedan 5 modelos en `MISSING`: 3 LoRA de MiniMax H3 en fal (fase 8), FLUX 3 `draft-enhance` (necesita el `draft_cache_url` de un borrador previo) y `flux-3-action/so101` (robótica, fuera de las familias creativas).

En `tests/fixtures/live/expected.txt`, `MISSING:` marca los modelos que aún no pueden ejecutarse porque exigen algo que la app no envía (21 casos: máscaras, `keyframes`, `video_clips`, audio, LoRA, la imagen de origen de Ideogram Character junto a sus referencias…). `ERROR` marca los esquemas que el proveedor no sirve: fal `minimax/h3-max/director` devuelve 404. Cada tarea siguiente debe reducir esos casos, y el diff lo deja registrado.
